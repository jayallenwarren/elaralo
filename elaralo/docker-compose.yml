version: "3.9"
services:
  elaralo:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./persist/data.db
    volumes:
      - dbdata:/app/backend/persist
volumes:
  dbdata:
